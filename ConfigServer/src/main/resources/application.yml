server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER
    
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ChinmayDeshmukh20/hotel-microservice-config
          clone-on-start: true    
  